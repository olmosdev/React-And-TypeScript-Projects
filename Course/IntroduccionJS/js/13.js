// Function declaration

function makeSum(firstNumber = 0, secondNumber = 0) { // Receiving arameters
    console.log(firstNumber + secondNumber);
}

makeSum(10, 5); // Sending arguments
makeSum(10);
